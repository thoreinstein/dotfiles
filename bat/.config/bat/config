# Bat configuration file

# Set the theme to rose-pine-moon
--theme="rose-pine"

# Show line numbers, Git modifications and file header
--style="numbers,changes,header"

# Use italic text on the terminal (not supported on all terminals)
--italic-text=always

# Add mouse scrolling support in less (does not work with older
# versions of "less")
--paging=auto

# Use C++ syntax for .ino files
--map-syntax "*.ino:C++"

# Use YAML syntax for .yml and .yaml files
--map-syntax "*.yml:YAML"
--map-syntax "*.yaml:YAML"

# Use JSON syntax for .json files
--map-syntax "*.json:JSON"

# Wrap text at terminal width
--wrap=auto
